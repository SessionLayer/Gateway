{
  "cp_mtls_endpoint": "${CP_MTLS_ENDPOINT}",
  "data_dir": "${GW_DATA_DIR}",
  "bootstrap": {
    "enrollment_token": "${GW_ENROLL_TOKEN}",
    "ca_cert_path": "${GW_CA_PEM}",
    "gateway_name": "${GW_NAME}",
    "server_name": "${CP_SERVER_NAME}"
  },
  "identity": {
    "connect_timeout_secs": 30,
    "rpc_timeout_secs": 30
  },
  "ssh": {
    "listen_addr": "${GW_SSH_ADDR}",
    "cp_connect_timeout_secs": 30,
    "cp_rpc_timeout_secs": 30,
    "inner": {
      "connect_timeout_secs": 10,
      "handshake_timeout_secs": 15
    },
    "recorder": {
      "require_https": false
    },
    "agent": {
      "listen_addr": "${GW_AGENT_ADDR}",
      "advertise_url": "${GW_AGENT_ADVERTISE}"
    }
  },
  "ha": {
    "mode": "single_instance"
  }
}
