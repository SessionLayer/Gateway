{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sessionlayer-gateway.fullname" . }}
  labels:
    {{- include "sessionlayer-gateway.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sessionlayer-gateway.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        # The outer SSH leg, the agent transport agents dial in on, and /readyz
        # for the load balancer and the kubelet.
        - protocol: TCP
          port: {{ .Values.ssh.listenPort }}
        - protocol: TCP
          port: {{ .Values.ssh.agent.listenPort }}
        - protocol: TCP
          port: {{ .Values.ha.drain.readyzPort }}
    {{- with .Values.networkPolicy.extraIngress }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  egress:
    {{- with .Values.networkPolicy.dnsPodSelector }}
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              {{- toYaml . | nindent 14 }}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- end }}
    {{- with .Values.networkPolicy.controlPlanePodSelector }}
    # Authorize, the lock feed, recording lifecycle, certificate signing, renew.
    - to:
        - podSelector:
            matchLabels:
              {{- toYaml . | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ $.Values.networkPolicy.controlPlaneGrpcPort }}
    {{- end }}
    {{- with .Values.networkPolicy.nodeCidrs }}
    # The SSH nodes this Gateway bridges to, on top of the in-process
    # loopback-only splice and the node-dial validation.
    - to:
        {{- range . }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
      ports:
        - protocol: TCP
          port: 22
    {{- end }}
    {{- with .Values.networkPolicy.wormCidrs }}
    # Recording uploads. The bytes go straight from here to the object store.
    - to:
        {{- range . }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
      ports:
        - protocol: TCP
          port: 443
    {{- end }}
    {{- with .Values.networkPolicy.extraEgress }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
